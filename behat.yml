default:
  suites:
    default:
      paths: [features]
      contexts:
        - FeatureContext
  extensions:
    Behat\MinkExtension:
      default_session: goutte
      base_url: http://crazyblog